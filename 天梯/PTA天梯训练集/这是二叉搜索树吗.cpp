#include <bits/stdc++.h>
using namespace std;
struct Node
{
    int data;
    Node *left = NULL;
    Node *right = NULL;
};
Node* creat(int n)
{
    
}
int main(void)
{
    int n;
    cin>>n;
    Node* root=creat(n);
    return 0;
}