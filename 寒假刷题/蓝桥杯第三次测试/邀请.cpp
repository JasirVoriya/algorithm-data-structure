#include<bits/stdc++.h>
using namespace std;
const int maxn=1e5+10;
struct node{
    int m,s;
};
node a[maxn];
bool cmp(node& a,node& b)
{
    if(a.m==b.m)return a.s>b.s;
    return a.m<b.m;
}
int main(void)
{
    int n,d;

    return 0;
}